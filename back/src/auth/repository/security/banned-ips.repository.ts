// TODO ADD CRUD
// +
// banIp(ip: string, reason: string): Promise<void>
// unbanIp(ip: string): Promise<void>
// isIpBanned(ip: string): Promise<boolean>
// getBanReason(ip: string): Promise<string | null>
